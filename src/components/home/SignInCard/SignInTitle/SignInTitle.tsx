import { Typography } from "@mui/material";

const SignInTitle = () => (
  <>
    <Typography variant="h2">Sign In</Typography>
    <Typography variant="body1">
      Fill in the fields bellow to sign into your account
    </Typography>
  </>
);

export default SignInTitle;
